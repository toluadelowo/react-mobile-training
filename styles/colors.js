import { StyleSheet } from "react-native"

export default StyleSheet.create({
    blue: {
        color: "#acbaac"
    },
    red: {
        color: "#eeaacc"
    },
    green: {
        color: "#bbaadd"
    }
})